import { Paper } from '@mantine/core'
import { AppText } from '@the-one/ui'

export const OverviewRecentSales = () => {
  return <Paper radius={'lg'} shadow="sm" p={24} withBorder>
    <AppText fontWeight='500'>Recent Sales</AppText>
    
  </Paper>
}
